import { StyleSheet } from 'react-native';
import { backgroundColor } from 'src/common/theme/variables';

export default StyleSheet.create({
	container: {
		justifyContent: 'center',
		padding: 20,
		backgroundColor: 'rgba(0,0,0,0)',
	},
	containerMenu: {
		marginTop: 20,
	},
	button: {
		marginBottom: 20,
		paddingHorizontal: 20,
		paddingVertical: 18,
	},
});
